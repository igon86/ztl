#include "permserver.h"

//PILA DI INTERAZIONE tra worker e writer

int main(int argc,char *argv[]){
	
	/* primo controllo validita argomenti*/
	if(argc == 0) exit(EXIT_FAILURE);
	
	//CONTATTARE IL SERVER SULLA SOCKET
	
	
}